.container
    h1 {{kit.kitName}}
    .row
        .col-md-6
            h3(ng-show="kit.inStock") In Stock
            h3 Aquired on {{kit.aquiredDate | date}}

        .col-md-6
            .panel.panel-primary
                .panel-heading Tags
                .panel-body
                    div(ng-repeat="tag in kit.tags") {{tag}}
